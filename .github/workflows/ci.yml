name: CI

on:
  push:
  pull_request:

jobs:
  cmake:
    name: CMake ${{ matrix.os }} ${{ matrix.build_type }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest
        build_type:
          - Debug
          - Release

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache CMake build
        uses: actions/cache@v4
        with:
          path: cmake-build
          key: cmake-${{ matrix.os }}-${{ matrix.build_type }}-${{ hashFiles('CMakeLists.txt', '**/*.cmake') }}
          restore-keys: |
            cmake-${{ matrix.os }}-${{ matrix.build_type }}-
            cmake-${{ matrix.os }}-

      - name: Configure (Unix)
        if: runner.os != 'Windows'
        run: >
          cmake -S . -B cmake-build
          -G Ninja
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
          -DBUILD_TESTS=ON
          -DBUILD_EXAMPLES=ON
          -DBUILD_PERF=ON

      - name: Configure (Windows)
        if: runner.os == 'Windows'
        run: >
          cmake -S . -B cmake-build
          -G Ninja
          -DCMAKE_C_COMPILER=clang
          -DCMAKE_BUILD_TYPE=${{ matrix.build_type }}
          -DBUILD_TESTS=ON
          -DBUILD_EXAMPLES=ON
          -DBUILD_PERF=ON

      - name: Build
        run: cmake --build cmake-build --parallel

      - name: Test
        run: ctest --test-dir cmake-build --output-on-failure

  windows-batch:
    name: Windows batch runner
    runs-on: windows-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check Clang
        run: clang --version

      - name: Run legacy test script
        run: cmd /c build\build.bat all test

      - name: Run performance benchmark
        run: cmd /c build\build.bat perf run
