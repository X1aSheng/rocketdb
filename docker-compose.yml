version: "3.9"

services:
  rocketdb-test:
    build:
      context: .
      dockerfile: Dockerfile
    image: rocketdb:latest
    container_name: rocketdb-test
    command: >
      sh -c "ctest --test-dir /build --output-on-failure -V &&
             echo '=== All tests passed ==='"
