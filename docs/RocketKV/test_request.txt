嵌入式架构领域的专家，保持严谨慎重的态度，语言不要夸大，进行内部分析模拟测试提供的驱动代码。
完全掌握W25Q128类型NOR flash的特性和操作逻辑，详细追踪内存状态。

Rocketdb驱动代码，分为以下文件结构：
rocketdb.h
rocketdb_kvdb.c
rocketdb_tsdb.c

分析测试数据，列出缺陷和故障，进行算法评估和改进建议。

主要集中处理RocketDB的功能实现和稳定性的修复和优化，对KVDB和TSDB算法进行模拟运算，进行可靠性与性能评估，无需关心外部接口（CRC16-modbus、CRC32、Flash操作等）实现。

实际使用时会将Flash芯片的整个空间分为不同子分区进行使用，一般情况一个子分区大小不会超过1MB。

测试参数：使用芯片W25Q128，默认不开启索引机制
KVDB1:32KB
KVDB2:64KB
TSDB1:32KB
TSDB2:64KB

确保每个分区触发至少100次GC，验证不同写入/删除模式下的GC行为

未收到指令不要代码进行输出。

不考虑向后兼容，使用C99标准，禁止使用bool变量，结构体对齐并注释字节数，优化并修复代码，逐个文件输出，输出一个文件完成后等待指令，更新说不要包含在驱动代码文件内，最后总结为单个readme文件

进行测试

目前的数据结构是否可以优化缩小内存占用，CRC使用外部API，且算法为CRC16-MODBUS，无需在驱动内实现，

分析完成后，进行整体改进和修复建议,未收到指令不要代码进行输出。


接下来，我将提供修复版的代码，准备进行回归测试，重点测试怀疑故障点，接收代码，等待指令


目前的数据结构是否可以优化缩小内存占用，CRC使用外部API，无需在驱动内实现，是否可以使用CRC32，分析完成后，进行整体改进和修复建议


为节省输出Token，未收到明确指令时不要进行代码输出


是否还有未测试到的场景，KVDB、TSDB测试中还要增加混合不定KEY、data长度的测试，KEY和VAL长度都要随机混合测试，请设计好测试方法

关于整体性能改进还有什么建议吗？


结合后面提交的测试报告，再次重点测试高风险项

关于分析出的修复点，详细分析是否存在影响，是否有更好的改进措施

参考FlashDB（https://github.com/armink/FlashDB）、LittleFS（https://github.com/littlefs-project/littlefs）、以及RocketDB代码文件，完全优化重构的KVDB和TSDB，命名为RocketDB，版本号：0.0.1，可靠性与稳定性为基本要求。

现有实现算法还需要进一步优化，结合现有代码，无需兼容旧版本，保留API，重构整个驱动，优化翻转环绕方式，科学使用合适算法进行设计，特别是数据结构。

KVDB1:32KB
TSDB1:32KB
保证整个分区内的所有扇区都重新GC一次，详细追踪分区数据状态


我觉得你的优化思路还需要修改，KVDB的整体存储结构不一定是环形的，在第一次存储满之后，发生删除或者GC操作，整理出的空闲扇区不一定能翻转到存储区的头部，有可能是存储区的中部或者为尾部，所以这个数据结构的设计需要优化

无需兼容旧版本，逐个输出修复改进后的完整代码文件，完成后等待指令


逐个输出改进后的完整代码文件，首先输出头文件

控制单次输出token限制在4K
注意控制单次token用量


将所有修复经验内化为设计规则的一部分，消除"补丁式"标记，让文件读起来像一份从零开始就考虑周全的成熟设计文档。

接收3个文件，分析后，准备将驱动分拆为 rocketdb_kvdb.h/.c 和 rocketdb_tsdb.h/.c，不要修改结构体命名，并完善注释，文件逐个输出，首先输出KVDB
等待指令执行
